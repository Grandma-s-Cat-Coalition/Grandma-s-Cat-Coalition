// SHELTERLUV_API_KEY is optional: without it the adopt page uses ShelterLuv's embed widget.
const keys=['RESEND_API_KEY','FORM_FROM_EMAIL','CONTACT_RECIPIENT_EMAIL','RESEND_AUDIENCE_ID','OAUTH_GITHUB_CLIENT_ID','OAUTH_GITHUB_CLIENT_SECRET','SITE_URL'];const bad=keys.filter(k=>!process.env[k]||/example|replace|placeholder/i.test(process.env[k]));if(bad.length){console.error(`Missing deployment variables: ${bad.join(', ')}`);process.exit(1)}
