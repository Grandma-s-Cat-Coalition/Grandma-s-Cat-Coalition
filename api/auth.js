export default function handler(req,res){const id=process.env.OAUTH_GITHUB_CLIENT_ID,site=process.env.SITE_URL;if(!id||!site)return res.status(503).send('OAuth is not configured');const state=crypto.randomUUID();res.setHeader('Set-Cookie',`oauth_state=${state}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=600`);const callback=new URL('/api/callback',site);res.redirect(`https://github.com/login/oauth/authorize?client_id=${encodeURIComponent(id)}&redirect_uri=${encodeURIComponent(callback)}&scope=repo&state=${state}`)}
