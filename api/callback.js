export default async function handler(req,res){const state=(req.headers.cookie||'').match(/oauth_state=([^;]+)/)?.[1];if(!state||state!==req.query.state)return res.status(400).send('Invalid OAuth state');const r=await fetch('https://github.com/login/oauth/access_token',{method:'POST',headers:{accept:'application/json','content-type':'application/json'},body:JSON.stringify({client_id:process.env.OAUTH_GITHUB_CLIENT_ID,client_secret:process.env.OAUTH_GITHUB_CLIENT_SECRET,code:req.query.code})});const data=await r.json();if(!data.access_token)return res.status(401).send('Authorization failed');const payload=JSON.stringify({token:data.access_token,provider:'github'}).replace(/</g,'\\u003c');res.setHeader('content-type','text/html; charset=utf-8');res.send(`<!doctype html><script>window.opener.postMessage('authorization:github:success:${payload}','${new URL(process.env.SITE_URL).origin}');window.close()</script>`)}
