const authHeaders = key => ({ Authorization: `Bearer ${key}`, 'X-Api-Key': key });

const animalUrl = a => a?.ID ? `https://new.shelterluv.com/matchme/adopt/GCCI/Cat/${encodeURIComponent(a.ID)}` : '';

export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});if(!process.env.SHELTERLUV_API_KEY)return res.status(503).json({error:'ShelterLuv is not configured'});try{const r=await fetch('https://new.shelterluv.com/api/v1/animals?status_type=publishable',{headers:authHeaders(process.env.SHELTERLUV_API_KEY)});if(!r.ok)throw 0;const raw=await r.json(),animals=raw.animals||raw;const cats=animals.filter(a=>!a.Type||/cat/i.test(a.Type)).map(a=>({id:a['Internal-ID']||a.ID,name:a.Name,photo:a.CoverPhoto,age:a.Age,sex:a.Sex,breed:a.Breed,description:a.Description,profileUrl:animalUrl(a)}));res.setHeader('Cache-Control','s-maxage=600, stale-while-revalidate=3600');return res.status(200).json(cats)}catch{return res.status(502).json({error:'Unable to load animals'})}}
