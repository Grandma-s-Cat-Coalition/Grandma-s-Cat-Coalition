export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const email=req.body?.email;if(!/^\S+@\S+\.\S+$/.test(email||''))return res.status(400).json({error:'Valid email required'});if(!process.env.RESEND_API_KEY||!process.env.RESEND_AUDIENCE_ID)return res.status(503).json({error:'Newsletter is not configured'});const r=await fetch(`https://api.resend.com/audiences/${process.env.RESEND_AUDIENCE_ID}/contacts`,{method:'POST',headers:{authorization:`Bearer ${process.env.RESEND_API_KEY}`,'content-type':'application/json'},body:JSON.stringify({email,unsubscribed:false})});return res.status(r.ok?200:502).json(r.ok?{ok:true}:{error:'Could not subscribe'})}
